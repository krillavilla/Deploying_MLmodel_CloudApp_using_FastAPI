import setuptools

setuptools.setup(
    name="app",
    version="0.0.0",
    description="Starter code.",
    author="Student",
    install_requires=[],  # No dependencies required for setup
    python_requires=">=3.8",
)
